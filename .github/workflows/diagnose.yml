name: Diagnose bot

on:
  workflow_dispatch:

jobs:
  diagnose:
    if: github.actor == 'osipovdenis-cell'
    runs-on: [self-hosted, home-deluxe-bot]
    timeout-minutes: 10
    steps:
      - name: Check service, tests and recent errors
        run: sudo /usr/local/sbin/diagnose-home-deluxe-bot
